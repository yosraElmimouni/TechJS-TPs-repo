{
  "name": "book-tracker-frontend",
  "version": "1.0.0",
  "description": "Frontend for the Book Reading Tracker TP",
  "main": "index.html",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
